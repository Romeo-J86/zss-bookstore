spring:
  application:
    name: @project.artifactId@
  datasource:
    url: jdbc:postgresql://localhost:5432/bookstore
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

transaction:
  api:
    url: https://lab.v.co.zw/interview/api/transaction
    token:  9ca3d5ed-dc04-4700-8dd6-7d60c3cdf0fa

server:
  port: 8085

